---
import ScrollReveal from './ScrollReveal.astro';

interface Props {
  class?: string;
  delay?: number;
}
const { class: className = '', delay = 0 } = Astro.props;
---

<ScrollReveal variant="fade-up" delay={delay} class={className}>
  <slot />
</ScrollReveal>
